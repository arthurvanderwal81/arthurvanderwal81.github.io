class TaaSAPI
{
    static sendVerificationCode(phoneNumber)
    {/*
        return fetch("https://www.test.com/?q=" + phoneNumber, {
            "method": "GET",
            "mode": "no-cors"
        })
        .then(response => response.json());
        */
    }
}